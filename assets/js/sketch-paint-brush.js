'use strict';

let brush, color_brush, size;
let width = 800;
let high = 700;

function setup() {
    cursor('default');
    let lienzo = createCanvas(width, high);
    lienzo.parent('contenedor-canvas');
    background(0);
    //textFont('Montserrat');
}

//Inicio draw()
function draw() {

}